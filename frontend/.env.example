# Base URL of the backend FastAPI server. Defaults to http://localhost:8010
# in code if this is left unset — only needed to point at a non-default host.
VITE_API_BASE_URL=http://localhost:8010

# Authentication uses an HttpOnly session cookie. No browser-side secret is required.
