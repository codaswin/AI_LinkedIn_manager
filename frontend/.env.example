# Base URL of the backend FastAPI server. Defaults to http://localhost:8000
# in code if this is left unset — only needed to point at a non-default host.
VITE_API_BASE_URL=http://localhost:8000

# Optional. Must match backend DASHBOARD_API_KEY when that guard is enabled.
VITE_DASHBOARD_API_KEY=
